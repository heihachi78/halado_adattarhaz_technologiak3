name: 'dbt_transform'
version: '1.0.0'

profile: 'dbt_transform'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_transform:
    facts:
      +materialized: table
    dims:
      +materialized: table
    marts:
      +materialized: table
